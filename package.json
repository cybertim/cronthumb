{
  "name": "cronthumb",
  "version": "1.0.0",
  "description": "Cron Job to generate Thumbnails with nice index.html for big Photo Dirs (like hosting your own cloud etc.)",
  "main": "run.coffee",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "app": "coffee src/run.coffee"
  },
  "author": "cybertim",
  "license": "GPL-3.0",
  "devDependencies": {
    "coffee-script": "^1.10.0"
  }
}
